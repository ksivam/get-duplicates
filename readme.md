Get duplicate strings
=====================
A “get duplicates” function that when given a Unicode string returns a string that contains a case-sensitive single instance of characters that are duplicated. 
Example: “AABBCC” returns “ABC”

Install:
========
npm install

Run:
===
- node app.js
- in browser: http://localhost:2121
